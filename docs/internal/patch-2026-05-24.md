# parallel patch-drafting experiment · 2026-05-24

23 swarm-code subagents — one per High-severity audit finding from
`audit-2026-05-24.md`. Each read the source, drafted a minimal patch
proposal at `/tmp/patches/<id>.md`. ~8 min wall, ~$1 in Kimi tokens.

## Self-triage (each swarm rated its own patch)

- **21/23 marked safe + high confidence**
- 2 marked needs-review (05 reintroduces 1s shell poll; 13 should be done with 14)

## Application results

- **17/21 safe patches applied automatically** (exact-text replacement)
- **4 failed** to apply: whitespace drift between proposal and source
  (04, 06, 08, 19) — proposals were correct but the model used slightly
  different indentation. Deferred for manual application.

## Verified

After applying the 17 + adding two missing `import Util` lines:
- `make smoke` ✓
- `make test-all` ✓ (8/8 passing)
- Live `swarm-code -p "Reply with: PATCHED_OK"` ✓

## What's fixed

- **Liveness** (1,2,3): receive blocks now have deadlines
- **Concurrency** (7): browser msg_id uses large random ints to avoid race
- **Shell injection** (9,10,11,12,14): `Util.shell_q` wraps prev unquoted args
- **Path traversal** (15): mcp_log_path sanitises server name
- **Nil-deref** (16,17,18,20): added `== nil` guards on getenv/choices/content
- **Logic** (21,22): markdown code-span vs bold precedence; mcp boot-deadline
- **TOCTOU** (23): `/tmp/swarm-code-last-body.json` uses `mktemp`-style path

## Remaining

- 05 (journal_sync atomic) — needs a non-shell rename primitive (sw runtime gap)
- 13 (log.sw tail_recent) — bundled with 14, already covered
- 04, 06, 08, 19 (whitespace drift) — apply manually next pass

## Honest verdict

Parallel patch drafting is a real workflow. Triage by self-declared
risk worked — the 17 auto-applied patches all built clean and didn't
break smoke/test. ~75% of drafted patches landed on the first try.
