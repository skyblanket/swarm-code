# swarm-code parallel audit · 2026-05-24

26 `swarm-code -p` headless subagents fired in parallel — one per
`src/*.sw` module. Each looked for: dead code, TODOs, hallucinated
comments, race conditions, missing error handling, shell-injection,
O(n²) accumulators.

**Wall-clock:** 9 minutes. **Token cost:** ~$1. **Total findings:** 202 bullets (23 High, ~100 Medium, ~80 Low).

## Executive summary — highest-impact bugs

These came back with verifiable `file:line` refs. Spot-checking
suggests >80% are real (not hallucinated). Ranked by blast radius.

### Liveness / deadlock risks
- `agent.collect_tool_result` blocks forever in `receive` with no timeout — hung tool freezes the REPL **[agent.sw:1566]**
- `agent.ask_via_reader` same — crashed reader deadlocks main agent **[agent.sw:1734]**
- `agent.run_subagent_loop` calls `LLM.chat` without timeout — hung subagent freezes parent forever **[agent.sw:1622]**
- `browser.wait_for_response` resets `timeout_ms` on every CDP event — steady event stream blocks indefinitely **[browser.sw:126]**

### Data-loss / corruption risks
- `agent.journal_sync` overwrites journal without atomic rename — crash mid-write truncates ALL of session history. **The comment claiming this is equivalent to the old rename behavior is a hallucination I introduced earlier today.** **[agent.sw:147]**
- `background.parse_int_safe` returns 0 for empty input — race-empty exit file treated as success **[background.sw:244]**
- `browser.next_msg_id` non-atomic read-modify-write on ETS — concurrent CDP calls corrupt request/response pairing **[browser.sw:97]**

### Shell injection
- `tools.shell_inner_quote` escapes only `"`, not `$` or backticks — command injection via path in `do_file_watch` **[tools.sw:1216]**
- `do_web_search` `max_results` unsanitized, injected into shell **[tools.sw:904]**
- `do_log_wait` + `do_file_watch` `timeout_sec` unsanitized, injected into `with_timeout` **[tools.sw:1149, 1190]**
- `memory.migrate_legacy` `shell("mv " ++ legacy …)` unquoted **[memory.sw:84]**
- `log.summarize` + `tail_recent` interpolate telemetry path unquoted — breaks on `$HOME` with spaces **[log.sw:166, 182]**

### Path traversal
- `mcp.mcp_log_path` interpolates unsanitized server name from user config — `../` lets a malicious MCP config write arbitrary files **[mcp.sw:82]**

### nil-deref / crash on bad input
- `llm.extract_content_impl` no nil guards on `choices` / `content` — malformed API response crashes process **[llm.sw:854, 858]**
- `main.sw` `getenv("HOME")` concatenated without guard **[main.sw:147]**
- `arthopod.hatch` + `generate_soul` same **[arthopod.sw:165, 201]**

### Logic bugs
- `markdown.inline_loop` checks `**` before `` ` `` → `` `**text**` `` renders as bold instead of code **[markdown.sw:530]**
- `mcp` boot deadline (45s) shorter than handshake timeout (60s) — slow server registered in ETS but never added to `all_servers`, invisible to schemas/prompts/shutdown **[mcp.sw:145]**

### Race / TOCTOU
- `llm` writes `/tmp/swarm-code-last-body.json` without `mktemp`/`O_EXCL` — symlink race in world-writable `/tmp` **[llm.sw:524, 622, 778]**

### Per-module finding counts (severity High)
- agent.sw               4 high
- arthopod.sw            2 high
- background.sw          2 high
- browser.sw             2 high
- llm.sw                 2 high
- log.sw                 2 high
- main.sw                1 high
- markdown.sw            1 high
- mcp.sw                 2 high
- memory.sw              1 high
- tools.sw               4 high

---

## Raw per-module reports

This is the raw output, per module. Review for false positives before
acting — but the signal is unusually high (the model used real
`file:line` references, not vibes).


---

# agent.sw audit

## High
- `collect_tool_result` blocks forever in `receive` with no timeout; a hung tool worker freezes the REPL [line 1566]
- `ask_via_reader` blocks forever in `receive` with no timeout; a crashed reader deadlocks the main agent [line 1734]
- `run_subagent_loop` calls `LLM.chat` without timeout; a hung subagent worker freezes its parent `collect_tool_result` forever [line 1622]
- `journal_sync` overwrites the journal file directly without atomic rename; a crash mid-write truncates the entire journal, and the comment claiming this is equivalent to the old rename behavior is wrong [line 147]

## Medium
- `encode_journal_loop` builds output via `acc ++ line` string concatenation, O(n²) on journal size, called after every turn and tool call [line 113]
- `execute_all` calls `journal_sync` after every individual tool call in a multi-tool chain, re-serializing the full history each time and increasing corruption window [line 1665]
- `dispatch_tool` runs `browser_*` tools inline without worker isolation; a panicking browser tool kills the REPL process [line 1535]
- `file_write` and `file_mkdir` return values are ignored everywhere (journal, sessions, profile override); silent write failures break crash recovery [lines 158, 253, 269, 272, 317, 320, 860, 876, 1173, 1176]
- `run_headless` duplicates the entire crash-recovery journal setup from `run`; fixes in one path are likely to be missed in the other [line 297]
- `main_loop`, `run_turn`, `execute_all`, and `run_subagent_loop` are tail-recursive; if swarmrt lacks TCO, long sessions will stack-overflow [line 362]

## Low / nit
- `sum_msg_chars` is defined but never called; dead code [line 1068]
- `handle_eof` returns `history` after `sys_exit(0)`; unreachable dead code [line 564]
- `clear_profile_override` shells out `rm -f` instead of using `file_delete` [line 882]
- `append_all`, `take_first`, `history_to_objects`, `decode_history_loop`, `replay_lines`, `collect_session_names`, `filter_gt`, `filter_le`, and `format_for_summary` all use `list_append` or string `++` in accumulation loops, O(n²) [lines 1132, 1119, 1184, 1229, 170, 1259, 1282, 1291, 1151]
- `session_dir` assumes `getenv("HOME")` is non-nil; if unset, constructs an invalid path [line 93]
- `cognitive_pulse` drops the pulse user-message from history when the model returns `idle`; the model forgets prior pulses [line 463]
- `main_loop` wakes every 15 s via `after 15000` to call `on_idle`, which is a no-op; unnecessary timer churn [line 374]


---

# arthopod.sw audit

## High
- `shell_escape` only strips `'` and `\n`, leaving backslashes that can break out of the single-quoted `$USER` payload in the `shasum` pipeline [line 113]
- `hatch` and `generate_soul` concatenate `getenv("HOME")` without nil guard, passing invalid paths to `file_exists`, `file_mkdir`, and `file_write` [lines 165, 201]

## Medium
- `dashes` recursively returns `""` for every `n > 0`, so `render_with_bubble` prints empty top/bottom borders instead of horizontal rules [line 355]
- `extract_soul_json` declares unused `start_marker` / `end_marker`; comment claims to search for a `{ ... }` block but only calls `json_decode` [line 209]
- `render_with_bubble` comment says "speech bubble box" but emits only spaces—no corners, vertical bars, or horizontal rules [lines 328-336]
- `address_response` interpolates LLM-generated `name` and `personality` into the system prompt without escaping, allowing prompt injection via a crafted soul JSON [line 409]
- `hatch` does not validate that the decoded soul map contains required keys (`name`, `personality`), so missing keys propagate as `nil` through rendering [line 170]

## Low / nit
- `hat_line` returns invisible spaces for 6 of 8 hat types, making the feature non-functional [line 293]
- `wrap_text` truncates instead of wrapping; comment admits "Real wrapping across lines later" [line 346]
- `list_nth` will crash on an empty list; no guard [line 140]
- `render` assumes `sprite` always returns ≥3 elements without guard [lines 304-308]


---

# background.sw audit

## High
- `parse_int_safe` returns 0 for empty strings and any input starting with a non-digit, so a tampered or race-empty exit file is treated as success [lines 244–258, invoked at 215]
- `shell(detach_cmd)` return value is discarded; if the wrapper fails to start no process runs but the task is orphaned in `pending` forever [line 83]

## Medium
- ETS entries and `/tmp` log/pid/exit files for completed tasks are never cleaned up; `next_id` grows without bound [general]
- `sleep(20)` fixed delay before reading `pid_file` is brittle under load; should poll until the file appears [line 89]
- `kill_task` sets status to `'killed'` without writing an `/exit` value, so `result` later prints `exit nil` [lines 175, 119]
- `bg_done` notification is silently dropped if `whereis('main_agent')` returns `nil` at heartbeat tick time [line 223]
- `tail_log` splices `to_string(n_lines)` directly into a shell command without local validation [line 145]

## Low / nit
- Hallucinated/leftover comment: "Shell-escape helper (single-quote wrap, escape embedded quotes)." appears directly before `status` with no helper defined [line 102]
- `list_loop` accumulates strings with `acc ++ line`, giving O(n²) cost for large task lists [line 194]


---

# browser.sw audit

## High
- `wait_for_response` recurses with the original `timeout_ms` on every event frame, so a steady stream of CDP events can delay the matched response indefinitely [line 126]
- `next_msg_id` does a non-atomic read-modify-write on ETS `'next_id`; concurrent `cdp_call` invocations on the same session can reuse IDs and corrupt request/response pairing [line 97]

## Medium
- ETS field `'launched` is set to `'true` in `init` but never read; the `close()` comment claims "optionally kill chrome" yet it never terminates the process [lines 84, 336]
- `shell_quote_local` and `shell_quote_loop` are defined but never called — dead code [line 232]
- `file_write(p, png)` in `screenshot()` is unchecked; if the parent directory is missing the PNG is silently lost [line 227]
- `js_esc_loop` builds the escaped string with `acc ++ esc`, an O(n²) immutable-string accumulator; large `type_text` payloads will bottleneck [line 367]
- `Page.enable` and `Runtime.enable` in `init()` are fire-and-forget; CDP failures during setup are silently ignored [line 65]

## Low / nit
- `eval_string` returns `""` for both JS `null` and `undefined`, losing the distinction between them [line 330]
- `escape_js_string` omits `\t`, `\b`, `\f`, `\0` and other control characters [line 354]
- `map_get(page, 'webSocketDebuggerUrl')` can return `nil`; `to_string(nil)` becomes `"nil"` and is passed to `wsc_connect` instead of failing early [line 75]


---

# config.sw audit

## High
_none_

## Medium
- Comment claims `shell_q_local` is used, but code calls `Util.shell_q` [line 295]
- Hook `cmd` from settings.json is interpolated into shell command without escaping, allowing arbitrary execution via a malicious project config [line 302]

## Low / nit
- `getenv("HOME")` concatenated unchecked; nil HOME crashes path construction [line 41]
- Hook lists from settings.json are not validated as lists before `hd`/`length`; malformed JSON crashes the agent [line 247]
- Dangling comment references non-existent `shell_q_local` function with no code following it [line 308]


---

# heartbeat.sw audit

## High
_none_

## Medium
- Spawned heartbeat process is unlinked/unmonitored; a crash in `heartbeat_loop` silently kills the pulse with no restart or notification to main [line 18]

## Low / nit
- `as_prompt_section` calls `ets_get(table, 'tick_sec')` without a nil guard on `table`; a failed start yields `"nil seconds"` in the system prompt [line 82]
- `tick_sec` is not validated before `sleep(tick_sec * 1000)`; zero or negative values would busy-loop or crash [line 23]
- `active` is written once and never updated; process liveness must be inferred from table existence, not the `active` field [line 17]


---

# llm.sw audit

## High
- `extract_content_impl` derefs `length(choices)` and `map_get(msg_obj, 'content')` without nil guards — a malformed API response missing `choices` or `message` crashes the process [lines 854, 858]
- Hardcoded `/tmp/swarm-code-last-body.json` written without temp-file safeguards (mktemp, O_EXCL) — TOCTOU/symlink risk in a world-writable directory [lines 524, 622, 778]

## Medium
- `is_transport_truncated` is defined but never called inside the module and not exported [line 508]
- `chat_completions_url` passes `endpoint` straight to `string_ends_with` with no nil guard — a nil endpoint crashes [line 128]

## Low / nit
- Recursive list builders (`replace_at`, `messages_to_api_native`, `messages_to_api_inband`, `tool_calls_to_api`, `inband_tc_loop`, `inband_calls_to_internal`) accumulate with `list_append(acc, item)`, giving O(n²) growth [lines 208, 277, 339, 363, 420, 691, 704]
- `string_before_first` uses a hardcoded sentinel (`"\x01\x02SWPROSE\x02\x01"`) that will mis-split if the original text happens to contain those exact bytes [line 715]
- `extract_content_impl` is less defensive than the near-identical `extract_reasoning` (missing `choices == nil` and `msg_obj == nil` checks) [lines 854, 858]


---

# log.sw audit

## High
- `summarize()` interpolates telemetry path into shell string without quoting — breaks or injects if `$HOME` contains spaces [line 182]
- `tail_recent()` interpolates telemetry path into shell string without quoting — breaks or injects if `$HOME` contains spaces [line 166]

## Medium
- `event()` ignores `file_append` return — silent data loss if disk full or permissions deny write [line 58]
- Comment claims `session_start` logs opts snapshot, but function logs model/endpoint/cwd/pid [line 12]
- Comment lists `heartbeat` as a logged event, but no constructor or call site exists for it [line 21]
- Stale comment claims daily `{date}.jsonl` rotation, but `path()` returns flat `events.jsonl` [line 8]
- `tail_recent` docstring refers to "today's telemetry", but path is flat `events.jsonl` [line 160]

## Low / nit
- `truncate()` does not guard against `max_len < 3`, passing negative length to `string_sub` [line 205]
- `init()` ignores `file_mkdir` return value [line 40]
- `tail_recent()` and `summarize()` ignore `shell()` exit codes, swallowing non-zero errors from `tail`/`grep` [line 167, 195]


---

# main.sw audit

## High
- `getenv("HOME")` concatenated without nil guard; if `HOME` is unset, `manifesto_path` crashes or misbehaves. [line 147]

## Medium
- Header comment claims default model is `google/gemma-4-31B-it`, but `load_opts` defaults to `kimi-k2.6`. [line 17]
- Header comment documents `SWARM_CODE_MAX_TOKENS` and `SWARM_CODE_OUTPUT_RESERVE`, yet `load_opts` reads `SWARM_CODE_MAX_OUTPUT_TOKENS`; the former two are never referenced in main.sw. [line 19]
- `verify_network_isolation` comment says endpoint defaults to `http://sushi:8000`, but `load_opts` default is `https://api.moonshot.ai/v1/chat/completions`. [line 588]
- `import Telemetry` is unused in main.sw. [line 45]
- `read_all_stdin` recurses per line; piping megabytes of stdin risks stack exhaustion. [line 295]

## Low / nit
- `sys_exit(1)` makes the trailing `"denied"` return unreachable. [line 634]
- `Config.load()` invoked twice per startup (in `main` and again inside `load_opts`). [line 101, 394]
- `detect_tool_format` matches substrings that can misfire (e.g. `myz.ai` → native). [line 540]
- `import Vision` is unused in main.sw (no `Vision.*` calls). [line 41]
- `opts3c` rebound five times in sequence; a single `map_put` chain or map literal would be clearer. [lines 159–167]


---

# markdown.sw audit

## High
- `inline_loop` checks `peek2` for `**` before `peek1` for `` ` ``, so `` `**text**` `` renders as bold instead of a code span. [line 530]

## Medium
- `is_ansi_end_char` omits CSI terminators (`q`, `@`, `[`, `]`, `^`, `_`, `` ` ``, `{`, `|`, `}`, `~`); if `UI` emits sequences ending in these, `display_width` eats subsequent display characters. [line 675]
- `render_table` accepts a `width` parameter but never uses it, so wide tables overflow the terminal without truncation or wrapping. [line 357]
- Repeated string/list concatenation in hot loops creates O(n²) growth on large inputs: `code_indent_loop` [337], `join_loop` [273], `render_body_rows` [499], `inline_loop` [541], `set_last_loop` [712].

## Low / nit
- `render_quote` passes `width - 2` to `wrap_with_prefixes`, double-counting the 2-space indent already in the `bar` prefix and reducing available width by 2 columns. [line 324]
- `indent_line` parameter `width` is unused. [line 689]
- `render_code` parameter `width` is unused. [line 329]
- Empty bullet lines (`- `) produce `bullet_text("")`, which causes `wrap_loop` to return `""` with no bullet marker emitted. [line 315]
- `header_depth` requires a trailing space after hashes, so `#Header` falls through to paragraph instead of rendering as a header. [line 215]
- Top-level comment claims "every output line is wrapped at `width` columns", but code blocks and tables are not wrapped. [line 34]


---

# mcp.sw audit

## High
- Boot deadline (45 s) can be shorter than handshake timeout (60 s) + spawn time, causing `mcp_collect_boots` to time out while workers are still running; server gets registered in ETS but never appended to `all_servers`, so it is invisible to schemas, prompts, and shutdown. [line 145]
- Path traversal in `mcp_log_path`: unsanitized `server` name from user config is interpolated into the log path, allowing arbitrary file creation via `../`. [line 82]

## Medium
- `mcp_owner_loop` has no crash isolation; if `mcp_do_call` throws (bad JSON from server), the owner dies. `call_tool` sends to a dead pid and hangs for the full 260 s deadline before failing. [line 340]
- `mcp_extract_text` builds result by repeated string concatenation (`acc ++ sep ++ piece`), O(n²) in number of content blocks. [line 445]
- `mcp_safe_env_key` comment claims to reject all "shell-significant characters" but misses backslash, `#`, `(`, `)`, `*`, `?`, `!`, and others, allowing malformed env prefixes. [line 210]

## Low / nit
- Hallucinated comment: shutdown claims `handle_eof` calls `sys_exit(0)`, but no `handle_eof` function exists in this module or the visible codebase. [line 700]
- `getenv("HOME")` is unchecked; if `HOME` is unset, `mcp_log_path` concatenates `nil` and crashes. [line 81]
- `mcp_boot_worker` ignores the return value of `subprocess_send_line(handle, mcp_initialize_request())`; a broken pipe immediately after spawn fails silently on the subsequent read timeout instead of surfacing the write error. [line 134]
- `mcp_collect_boots` drops late `{'mcp_boot_done', name}` messages in the parent mailbox when the deadline expires, leaving orphaned notifications. [line 145]


---

# memory.sw audit

## High
- Unsafe shell interpolation: `shell("mv " ++ legacy ++ " ...")` with unquoted `legacy` derived from `getenv("HOME")` [line 84]

## Medium
- `json_decode` result checked for `nil` but not verified as a map before `map_keys`/`map_values`; non-map JSON will misbehave [lines 78-81]
- `rebuild_index` claims "sorted newest-first" in its comment but `file_list` returns unsorted entries and no sort is applied [line 135]
- `rebuild_index` is a non-atomic read-modify-write on `MEMORY.md`, reintroducing the RMW race the module design explicitly avoids for crumb files [line 138]
- `collect_md_paths` uses `list_append(acc, ...)` in recursion over directory entries — O(n²) on entry count [line 166]
- `render_index_lines` accumulates index text with `acc ++ entry` in recursion — O(n²) on total index size [line 177]

## Low / nit
- `update_index` accepts four parameters and ignores all of them [line 129]
- `migrate_legacy` comment says "delete the old file" but the code renames it to `.migrated` [line 71]
- `as_prompt_section` parameter `token` is unused [line 382]
- `is_slug_char` is a 36-case nested if/else ladder [line 290]
- `slug_clean` and `collapse_underscores` build output strings with `acc ++ x` recursion — O(n²) on input length [lines 286, 325, 328]
- `find_loop` returns `0 - 1` for not-found; reads as binary subtraction rather than a negative literal [line 263]


---

# prompts.sw audit

## High
_none_

## Medium
- `edit_desc` omits `replace_all` parameter present in `ToolSchemas.edit_s` [line 441]
- `multi_edit_desc` omits `replace_all` parameter present in `ToolSchemas.multi_edit_s` [line 465]
- `read_desc` omits `offset`/`limit` parameters present in `ToolSchemas.read_s` [line 428]
- `grep_desc` omits `glob`, `output_mode`, `head_limit` parameters present in `ToolSchemas.grep_s` [line 459]
- `web_fetch_desc` labels `prompt` as optional but `ToolSchemas.web_fetch_s` marks it required [line 483]

## Low / nit
- Repeated `++` string concatenation in `tool_descriptions`, `bash_desc`, and `rules` accumulates O(n²) copies [lines 93, 382, 499]


---

# reader.sw audit

## High
_none_

## Medium
_none_

## Low / nit
_none_


---

# scheduler.sw audit

## High
_none_

## Medium
- `save_all` ignores `file_write` result and writes non-atomically; a crash or disk-full loses state and `tick` will re-dispatch already-fired jobs on next heartbeat because `last_run` wasn't persisted [line 69]
- `dispatch` comment claims non-blocking, but `shell()` blocks the heartbeat thread until the bash wrapper exits (up to ~1 s due to swarmrt polling), so N due jobs can stall the heartbeat for N seconds [line 204]
- `dispatch` ignores `shell()` return value; a spawn failure is recorded as a successful run and the job won't retry until the next interval [line 221]
- `remove_loop` and `tick_loop` build lists with `list_append(acc, ...)` — O(n²) accumulator pattern [lines 119, 144]

## Low / nit
- `tick` accepts an `opts` parameter that is never used [line 137]
- `load()` only creates directories; the name implies it reads state [line 46]
- `tick` rewrites `schedule.json` on every heartbeat tick even when no jobs fired (the length guard is always true when jobs exist) [line 141]
- Hardcoded fallback binary path in `swarm_binary_path()` [line 230]
- `jobs_dir()` returns the telemetry directory, not a dedicated jobs directory [line 44]


---

# SessionSearch.sw audit

## High
_none_

## Medium
- Stale comment claims mtime-based incremental reindex, but code only checks presence in `meta` and active marker [line 20-22]
- Unhandled `db_open` failure in `init` and `search` leads to nil-deref or crash on corrupt/locked DB [line 41, 172]
- Active journal is read without atomicity guarantee against concurrent `journal_sync` writes; latest turn may be silently skipped [line 112-120]

## Low / nit
- O(n²) string accumulator in `render_hits` (`acc ++ entry`) [line 206]
- `ingest_one` and `ingest_tool_calls` call `to_string` on potentially-nil `map_get` results, indexing literal `"nil"` strings [line 142, 156-158]
- `ingest_lines` doesn't verify `json_decode` returned a map before calling `map_get` [line 132]


---

# skills.sw audit

## High
_none_

## Medium
- `rebuild_index` ignores `file_write` return value and returns `'ok` even when the index disk write fails [line 82]
- `file_list(dir)` return passed directly to `length()` without nil guard; a nil return (unreadable dir) crashes `collect_skill_paths` [line 78]
- `file_mkdir(dir)` in `save()` is unchecked; directory creation failure is masked by the subsequent file-write error [line 9]

## Low / nit
- `as_prompt_section(token)` parameter `token` is unused [line 213]
- `as_prompt_section` rebuilds the full index from disk on every prompt build despite skills rarely changing [line 214]
- `render_index_lines` uses string-append accumulator `acc ++ one_index_line(...)` giving O(n²) cost in total index size [line 104]
- `forget()` treats `nil` as a success value for `file_delete` alongside `'ok` and `'true` [line 54]


---

# telemetry.sw audit

## High
_none_

## Medium
- `mem_free_darwin()` hardcodes page size 16384; comment acknowledges 4096 on Intel but code never detects chip, inflating used/free by 4x on those machines. [line 45]

## Low / nit
- `disk_usage`, `mem_free`, `uptime_info`, `cpu_load` exported but only called internally by `sys_stats()`; no external consumers. [line 11]
- `df -h .` omits POSIX `-P`; narrow-terminal line wrapping can push numeric fields off `NR==2` and mis-parse disk stats. [line 64]


---

# test_runner.sw audit

## High
_none_

## Medium
- `bool_and` evaluates both arguments eagerly (function-call semantics), so `hd(parts)` in `t_split_empty` and `t_split_basic` will panic if the length check fails, aborting the whole suite instead of reporting a failure [lines 94, 112, 131]
- `t_journal_roundtrip` comment claims `encode_journal omits system` but the code calls `LLM.build_request_body`, not `encode_journal` [line 244]
- `walk_for_tcs` parameter `i` is never read, suggesting an abandoned indexing approach [line 257]
- `t_glob` and `t_grep` pass `Tools.exec` output directly to `string_contains` without guarding against nil [lines 323, 328]

## Low / nit
- `repeat_str` builds strings with left-nested `++`, making `t_split_trailing` O(n²) for n=64 [line 89]
- Top comment claims a regression guard for "infinite-write loop" but no test in the suite covers it [line 19]
- Top comment references `Agents.sw` (plural) but the actual source file is `agent.sw` [line 16]
- `t_remember_body` hardcodes the probe filename `zz_remember_probe.md`, risking collision if two test processes run concurrently [line 354]


---

# ToolRegistry.sw audit

## High
_none_

## Medium
- `knows` exported but never called by any module — dead code [line 119]
- `all_tools()` rebuilds the 35-entry list on every lookup; O(n) allocation per tool call with no caching [line 31]

## Low / nit
- Header comment claims "string ↔ atom" mapping but only string→atom is implemented [line 26]
- `atom_lookup` returns nil if an entry lacks the `atom` key; no defensive validation [line 107]
- Migration TODO comment documents uncompleted consolidation of schemas, descriptions, and handlers [line 17]


---

# tools.sw audit

## High
- `shell_inner_quote` only escapes `"`, not `$` or backticks — command injection via path in `do_file_watch` [line 1216]
- `do_web_search` `max_results` unsanitized, injected directly into shell command [line 904]
- `do_log_wait` `timeout_sec` unsanitized, injected into `with_timeout` seconds arg [line 1149]
- `do_file_watch` `timeout_sec` unsanitized, injected into `with_timeout` seconds arg [line 1190]

## Medium
- `do_code_search` uses `rg && ... || grep` — ripgrep exiting 1 (no matches) falls through to redundant grep scan [line 1119]
- `do_web_fetch` shell pipeline has no timeout — large HTML or stuck `sed` can hang the agent [line 1374]
- `format_search_results` builds output via `acc ++ block` recursion — O(n²) on unbounded `max_results` [line 1233]
- `do_git_commit` empty `files: []` produces empty stage list instead of default `"."` [line 1048]

## Low / nit
- `do_web_fetch` writes to fixed `/tmp/swarm_code_fetch.html` — collision risk across concurrent processes [line 1369]
- `do_web_fetch` `file_write` and `file_delete` return values unchecked [line 1370, 1376]
- `do_todo_write` calls `print()` side-effecting stdout inside a tool handler [line 1313]
- `all_tools()` reconstructs a 40-element list on every `exec()` call — avoidable allocation [line 81]
- `join_lines` and `take_first_lines` use tail-append patterns that are O(n²) in line count [line 631, 641]
- `do_edit_impl` and `do_multi_edit` are read-modify-write races against external editors [line 403, 1252]
- `digit_of` implemented as 10-deep nested if/else instead of a lookup [line 206]


---

# ToolSchemas.sw audit

## High
_none_

## Medium
- Stale comment claims native `tool_calls` are transcoded to `call:NAME{JSON}`; structured end-to-end removed this [line 11]
- Comment claims "13 core tools" but `all_schemas()` exports 29 schemas [line 14]
- `session_search_s.limit` typed as string but handler does numeric comparison (`n > 30`) [line 274]

## Low / nit
- Section comment "session search" precedes `read_image_s`, not `session_search_s` [line 251]


---

# trajectory.sw audit

## High
_none_

## Medium
- `export_current` exports the live `history` (which includes the runtime system prompt) instead of the journal-filtered view, leaking the full system prompt into training data despite the module comment claiming it is excluded. [line 115-121]
- `file_write` and `file_append` return values are ignored in `export_all`, `export_one`, and `export_current`; disk-full or permission errors are silently lost and success stats are still returned. [lines 65, 104, 121]
- `parse_journal_lines`, `clean_messages`, and `clean_tool_calls` build lists with `list_append(acc, item)` in tail-recursive loops, making them O(n²) for large sessions. [lines 135, 154, 202]

## Low / nit
- `file_mkdir(exports_dir())` in `export_current` is redundant when the user supplies a custom `out_path` outside the exports directory. [line 116]
- `default_path()` could collide if two exports run in the same millisecond because `timestamp()` is not guaranteed unique. [line 49]
- `is_journal_file` does not guard against directories named `journal-*.jsonl`; `file_read` will return nil and skip, but a defensive `file_is_regular` check would be clearer. [line 88]


---

# ui.sw audit

## High
_none_

## Medium
- `tool_result`, `rationale_box`, `answer_box`, `status_line`, `spinner_start` accept string args without nil guards; `string_split`/`string_trim`/`print` may crash on nil [lines 169, 219, 225, 334, 347]
- `format_agent_row` calls `to_string(map_get(entry, 'role'))` without nil guard; inconsistent with adjacent fields and may crash [line 716]
- `pad_right` calls `string_sub(s, 0, 1)` when `width <= 1` without guarding empty `s` [line 737]
- `agent_color` / `djb2_hash` assume `name` is a string; nil would crash in `string_length` [line 501]

## Low / nit
- Dead function `brand_dim()` defined but never used [line 51]
- Dead function `agent_prefix()` defined but never used [line 549]
- Dead assignment `id_val` in `todo_render_item` [line 407]
- `input_divider()` comment claims "kept for slash commands that still reference it" but no callers exist in src/ [line 325]
- Exported but unused function `footer_hint()` [line 339]
- `render_agent_rows` uses O(n²) string concatenation (`acc ++ "\n" ++ line`) [line 704]
- `take_n_lines` uses O(n²) `list_append` in loop (bounded by n=8) [line 208]


---

# util.sw audit

## High
_none_

## Medium
_none_

## Low / nit
- `shell_q` lacks a type guard; passing a non-string term to `string_replace` will raise at runtime [line 14]


---

# vision.sw audit

## High
_none_

## Medium
- Stale comments claim vision defaults to false and profiles without the flag are refused, but `supports()` defaults to ON (returns `'true'` when no settings or profile flag are present) [lines 28, 113]
- `paste_from_clipboard` macOS path is broken: `POSIX file` receives a single-quoted path produced by `Util.shell_q`, which is invalid AppleScript syntax; clipboard paste fails on darwin despite the "cross-platform via osascript" claim [line 312]

## Low / nit
- `extract_scan` and `auto_attach_loop` use `list_append` on linked lists inside loops, yielding O(n²) behaviour when many image paths are present [lines 217, 292]
- `paste_from_clipboard` leaves temp files in `/tmp` after a successful attach (no cleanup) [line 331]
- `grow_path_back` has no total-character cap; for long prefixes without whitespace it walks back the entire string length per match [line 198]
- `read_as_data_url` base64-encodes arbitrarily large files with no size limit, risking memory bloat and oversized API payloads [line 35]

