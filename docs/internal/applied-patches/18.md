# Patch 18 — main.sw:147

## Problem
`getenv("HOME")` concatenated without nil guard; if HOME is unset, manifesto_path crashes or misbehaves.

## Current code
```sw
    manifesto_path = getenv("HOME") ++ "/.swarm-code/SWARM_MANIFESTO.md"
    manifesto_text = if (file_exists(manifesto_path) == 'true') {
        m = file_read(manifesto_path)
        if (m == nil) { "" } else { m }
    } else { "" }
```

## Proposed fix
```sw
    home_env = getenv("HOME")
    manifesto_path = if (home_env == nil) { "" } else { home_env ++ "/.swarm-code/SWARM_MANIFESTO.md" }
    manifesto_text = if (file_exists(manifesto_path) == 'true') {
        m = file_read(manifesto_path)
        if (m == nil) { "" } else { m }
    } else { "" }
```

## Risk
safe

## Confidence
high — minimal nil guard using the same `== nil` pattern as `hb_interval_env` two lines above.
