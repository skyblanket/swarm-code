# Patch 02 — agent.sw:1734

## Problem
`ask_via_reader` blocks forever in `receive` with no timeout; a crashed reader deadlocks the main agent.

## Current code
```sw
        idx = receive {
            {'picker_answer', i} -> i
        }
```

## Proposed fix
```sw
        idx = receive {
            {'picker_answer', i} -> i
            after 30000 { -1 }
        }
```

## Risk
safe

## Confidence
high — same `receive … after` syntax used at line 374; timeout returns `-1` which `interpret_picker` treats as `deny`, the conservative fallback.
