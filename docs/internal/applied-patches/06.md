# Patch 06 — background.sw:244

## Problem
parse_int_safe returns 0 for empty or non-digit input, causing race-empty or tampered exit files to be treated as success (exit_code == 0 → 'done').

## Current code
```sw
fun parse_int_safe(s) {
    parse_int_digits(s, 0, 0)
}

fun parse_int_digits(s, i, acc) {
    if (i >= string_length(s)) { acc }
    else {
        ch = string_sub(s, i, 1)
        d = if (ch == "0") { 0 }
            else { if (ch == "1") { 1 }
            else { if (ch == "2") { 2 }
            else { if (ch == "3") { 3 }
            else { if (ch == "4") { 4 }
            else { if (ch == "5") { 5 }
            else { if (ch == "6") { 6 }
            else { if (ch == "7") { 7 }
            else { if (ch == "8") { 8 }
            else { if (ch == "9") { 9 }
            else { 0 - 1 }}}}}}}}}
        if (d < 0) { acc }
        else { parse_int_digits(s, i + 1, acc * 10 + d) }
    }
}
```

## Proposed fix
```sw
fun parse_int_safe(s) {
    if (string_length(s) == 0) { 0 - 1 }
    else { parse_int_digits(s, 0, 0) }
}

fun parse_int_digits(s, i, acc) {
    if (i >= string_length(s)) { acc }
    else {
        ch = string_sub(s, i, 1)
        d = if (ch == "0") { 0 }
            else { if (ch == "1") { 1 }
            else { if (ch == "2") { 2 }
            else { if (ch == "3") { 3 }
            else { if (ch == "4") { 4 }
            else { if (ch == "5") { 5 }
            else { if (ch == "6") { 6 }
            else { if (ch == "7") { 7 }
            else { if (ch == "8") { 8 }
            else { if (ch == "9") { 9 }
            else { 0 - 1 }}}}}}}}}
        if (d < 0) { 0 - 1 }
        else { parse_int_digits(s, i + 1, acc * 10 + d) }
    }
}
```

## Risk
safe

## Confidence
high — localized helper with a single caller; -1 is already the established error convention for invalid exit codes.
