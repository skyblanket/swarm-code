# Patch 23 — llm.sw:524

## Problem
Fixed path `/tmp/swarm-code-last-body.json` is written in a world-writable directory without temp-file safeguards, allowing symlink/TOCTOU attacks.

## Current code
```sw
    file_write("/tmp/swarm-code-last-body.json", body)
```

## Proposed fix
```sw
    file_mkdir(getenv("HOME") ++ "/.swarm-code")
    file_write(getenv("HOME") ++ "/.swarm-code/last-body.json", body)
```

## Risk
safe

## Confidence
high — `file_mkdir` and `getenv("HOME")` are used throughout the codebase; moving the debug artifact from world-writable `/tmp` to the user-owned `~/.swarm-code` directory eliminates the symlink/TOCTOU race entirely. Same change needed at line 623 (`chat_inband`).
